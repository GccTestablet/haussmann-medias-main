{% extends 'shared/layout/layout_base.html.twig' %}

{% block content %}
<div class="card mb-4">
    <h2 class="card-header text-center">Profile {{ user.fullName }}</h2>
    <hr class="my-0">
    <div class="card-body">
        <ul>
            <li>Full name: {{ user.fullName }}</li>
            <li>Email: {{ user.email }}</li>
            <li>Roles: {{ user.roles|join(', ') }}</li>
            <li>Enabled: {{ user.enabled }}</li>
            <li>Last login: {{ user.lastLogin|datetime_format }}</li>
            <li>Last activity: {{ user.lastActivity|datetime_format }}</li>
            <li>Created at: {{ user.createdAt|datetime_format }}</li>
            <li>Updated at: {{ user.updatedAt|datetime_format }}</li>
        </ul>
        <div class="justify-content-between">
            {% if is_granted(constant('App\\Security\\Voter\\UserVoter::SAME_USER'), user) %}
                <a class="btn btn-success" href="{{ path('app_user_update', {'id': user.id}) }}">Update</a>
            {% endif %}
            {% if user == app.user %}
                <a class="btn btn-warning" href="{{ path('app_security_profile_change_password') }}">Change password</a>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}
