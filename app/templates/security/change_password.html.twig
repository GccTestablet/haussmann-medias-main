{% extends 'shared/layout/base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_rest(form) }}

    <button class="btn btn-warning" type="submit">
        Save
    </button>
    {{ form_end(form) }}
{% endblock %}

