{% extends 'shared/layout/base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <h1 class="h3 mb-3 font-weight-normal">Haussmann Medias - Register</h1>

    {{ form_start(form) }}
    {{ form_rest(form) }}

    <button class="btn btn-lg btn-primary" type="submit">
        Register
    </button>
    {{ form_end(form) }}
{% endblock %}
