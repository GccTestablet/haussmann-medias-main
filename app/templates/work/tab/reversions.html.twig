{% extends 'work/show.html.twig' %}

{% block content_inner %}
    <div class="card">
        <div class="card-header text-end">
            <a href="{{ path('app_work_reversion_manage', {'work': work.id}) }}" class="btn btn-sm btn-warning">
                {% trans from 'work' %}Manage reversions{% endtrans %}
            </a>
        </div>
        <div class="card-body">
            {% component data_table with {
                pagerResponse: pagerResponse
            } %}
            {% endcomponent %}
        </div>
    </div>
{% endblock %}