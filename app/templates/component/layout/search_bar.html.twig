<div {{ attributes }} class="position-relative">
    <input
            type="text"
            data-model="debounce(500)|query"
            placeholder="Search an artwork, contract, beneficiary ..."
            class="form-control"
    >

    {% if this.results|length > 0 %}
        <div data-loading="addClass(opacity-50)" class="mt-3 row">
            {% for result in this.results %}
                {{ dump(result) }}
                <div class="position-absolute">
{#                    <div class="justify-content-between align-items-center">#}
{#                        {% if result.domain is defined %}#}
{#                            <div class="tt-menu navbar-search-suggestion ps tt-empty" style="top: 100%; left: 0; z-index: 100; display: none">#}
{#                                <div class="tt-dataset tt-dataset-pages">#}
{#                                    {% if result.domain.name == 'pages' %}#}
{#                                        <p>{{ result.label }}</p>#}
{#                                    {% endif %}#}
{#                                </div>#}
{#                                <div class="tt-dataset tt-dataset-files">#}
{#                                    {% if result.domain.name == 'files' %}#}
{#                                        <p>{{ result.label }}</p>#}
{#                                    {% endif %}#}
{#                                </div>#}
{#                                <div class="tt-dataset tt-dataset-members">#}
{#                                    {% if result.domain.name == 'members' %}#}
{#                                        <p>{{ result.label }}</p>#}
{#                                    {% endif %}#}
{#                                </div>#}
{#                            </div>#}
{#                        {% endif %}#}
{#                    </div>#}
                    {%  %}
                </div>
            {% endfor %}
        </div>
    {% endif %}
    {% if this.results|length == 0 and query is not empty %}
        <div class="pt-3 ml-2 text-nowrap mx-auto">Oh! No result found matching "{{ query }}"</div>
    {% endif %}
</div>
